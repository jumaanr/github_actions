# github_actions
Repository to learn about GitHub actions

This repository is created to learn about GitHub actions. It contains a simple workflow that runs on every push to the main branch. The workflow consists of three steps: a welcome message, checking out the code, and listing the files in the repository. The workflow also reads the README file and prints its contents to the console.